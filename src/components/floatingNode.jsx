import React from "react";
import Avatar from "@material-ui/core/Avatar";

const FloatingNode = (props) => {
  return <Avatar color="secondary">{props.element}</Avatar>;
};

export default FloatingNode;
